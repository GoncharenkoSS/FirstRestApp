#PORT
server.port=8090

#DATA BASE
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/RestBD
spring.datasource.username=postgres
#spring.datasource.password=postgres
spring.datasource.password=111223

#HIBERNATE
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.show_sql=true
# hibernate.current_session_context_class=thread

#METHOD PATCH DELETE
spring.mvc.hiddenmethod.filter.enabled=true 

#CREATE TABLE person(
#id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
#name varchar(100) NOT NULL, age int, email varchar(100));

#INSERT INTO person(name, age, email) VALUES ('Tom', 25, 'tom@mail.ru');
#INSERT INTO person(name, age, email) VALUES ('Bob', 51, 'bob@mail.ru');
#INSERT INTO person(name, age, email) VALUES ('Katy', 25, 'katy@mail.ru');

#alter table person
#add column created_add timestamp,
#add column updated_at timestamp,
#add column created_who varchar;